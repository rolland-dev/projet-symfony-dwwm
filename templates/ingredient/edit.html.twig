{% extends 'base.html.twig' %}

{% block title %}Mise à jour ingredient{% endblock %}

{% block body %}

<div class="text-center mt-4">
    <h1 class="mt-4">Modification d'un ingredient</h1>

    <div class="d-flex justify-content-center">
        <div class="col-3 text-center">
            {{ form_start(form) }}
            <div class="my-custom-class-for-errors">
                {{ form_errors(form) }}
            </div>

            <div class="row">
                <div class="col">
                    {{ form_row(form.nom) }}
                </div>
                <div class="col" id="some-custom-id">
                    {{ form_row(form.prix)}}
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
